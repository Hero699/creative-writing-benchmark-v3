# Disabled: Repository uses GitHub Pages native branch deployment (main / gh-pages).
name: Manual Workflow
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Branch deployment is active."
